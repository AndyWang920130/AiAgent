package com.example.myapp.contants.enumeration;

/** Severity/style of a notification — maps to the four icon+colour types the frontend renders. */
public enum NotificationType {
    INFO,
    SUCCESS,
    WARNING,
    ERROR
}
